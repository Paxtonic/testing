export default function HomePage() {
  return (
    <div>
      <h1>Welcome</h1>
      <p>
        This is the main content area. It grows to fill the space between the
        navigation bar and the footer, so the layout always uses the full
        viewport height.
      </p>
    </div>
  );
}
